# Resources and Transformers

API resources are stored in `app/Http/Resources/` and are responsible for formatting model output into consistent JSON shapes.

Always use resources when returning models from controllers or services to ensure consistent fields and localization.
