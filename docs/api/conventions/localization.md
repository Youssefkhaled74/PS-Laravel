# Localization

The API returns localized messages based on the `Accept-Language` header. Supported locales: `en`, `ar`.

Validation messages and resources should read translated strings from `resources/lang/*`.
